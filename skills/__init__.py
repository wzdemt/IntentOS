"""skills / 任务级能力 —— 把原语组合成「一件事的完整答案」。

每个 skill 是一个模块，导出 `detect(**kwargs)` 或 `run(**kwargs)`，返回一份能直接
读的结构化结论。**不进 registry、不占原语名额** —— 跟原语是两层东西：

  · 原语（primitives/）—— **原子操作**：读/写/列/删，可执行、可审计、进册子。
  · skill（skills/）—— **组合能力**：交叉几条原语的信息 + 一套判据，
    回答「一件事」而不是「一个动作」。

为什么要做成**可执行的一次调用**、而不是「一份说明让 agent 照着调」：接入测试实测过，
读说明 + 按说明逐条探索的成本，比让 agent 拿原生工具自己干还贵。真正的省法只有
「一次任务一次调用」—— 把组合逻辑固化在代码里，agent 不必每次重想。

调用：`python adapters/cli.py skill <名字> [--args '{...}']`
"""
